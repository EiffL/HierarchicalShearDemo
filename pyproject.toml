[project]
name = "hierarchical-shear-demo"
version = "0.1.0"
description = "Cosmological weak-lensing inference demo: pseudo-Cl vs hierarchical IS"
requires-python = ">=3.10"
dependencies = [
    "jax",
    "numpyro",
    "matplotlib",
    "scipy",
]
